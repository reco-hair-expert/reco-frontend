const variables = {
  primaryColor: '#000000',
  secondaryColor: '#ffffff',
  spacing: '1rem',
  // Add any other variables that might be used in your tests
};

module.exports = {
  ...variables,
  __esModule: true,
  default: variables,
}; 